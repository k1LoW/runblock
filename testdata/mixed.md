# Mixed Blocks

This block has a command:

```sh cat
block with command
```

This block has no command (should use default):

```text
block without command
```

Another block with command:

```sh cat
another block
```
